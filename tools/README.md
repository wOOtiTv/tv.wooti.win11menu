# Store package builder

This directory contains development-only helper tools.

`build_plasmoid.py` creates the uploadable KDE Plasma package for OpenDesktop/KDE Store without bundling repository-only files such as screenshots or documentation.
